'use client';

import ProfilePage from '@/app/profile/page';

export default function AdminProfilePage() {
  // Reuse the existing profile UI; admin layout keeps sidebar persistent
  return <ProfilePage />;
}


