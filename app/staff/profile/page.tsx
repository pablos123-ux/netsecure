'use client';

import ProfilePage from '@/app/profile/page';

export default function StaffProfilePage() {
  // Reuse the existing profile UI; staff layout keeps sidebar persistent
  return <ProfilePage />;
}
